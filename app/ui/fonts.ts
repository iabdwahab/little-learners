import { Raleway, Outfit } from 'next/font/google';

export const raleway = Raleway({
  weight: ['500', '600'],
  subsets: ['latin'],
});

export const outfit = Outfit({
  weight: ['400', '500', '600'],
  subsets: ['latin'],
});
